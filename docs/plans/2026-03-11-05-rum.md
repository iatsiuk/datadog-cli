# RUM: events, applications, metrics, retention filters, replay, audiences

## Overview

Implement the `rum` command group for datadog-cli, providing CLI access to all
Datadog RUM (Real User Monitoring) APIs. Covers searching RUM events, managing
applications, RUM-based metrics, retention filters, session replay (playlists,
heatmaps, viewership), and audience management.

Uses `github.com/DataDog/datadog-api-client-go/v2` (datadogV2).

Depends on: foundation plan.

## Context

- SDK APIs used:
  - `datadogV2.RUMApi`: ListRUMEvents, SearchRUMEvents, AggregateRUMEvents, CRUD applications
  - `datadogV2.RumMetricsApi`: CRUD RUM-based metrics
  - `datadogV2.RumRetentionFiltersApi`: CRUD retention filters, reorder
  - `datadogV2.RumReplayPlaylistsApi`: CRUD playlists, manage sessions in playlists
  - `datadogV2.RumReplayHeatmapsApi`: CRUD heatmap snapshots
  - `datadogV2.RumReplaySessionsApi`: GetSegments
  - `datadogV2.RumReplayViewershipApi`: CRUD session watches, list watchers/history
  - `datadogV2.RumAudienceManagementApi`: connections, mappings, queries (accounts/users)

## Development Approach

- **Testing approach**: TDD (tests first)
- Complete each task fully before moving to the next
- **CRITICAL: every task MUST include new/updated tests**
- **CRITICAL: all tests must pass before starting next task**

## Testing Strategy

- Mock HTTP responses using `httptest.Server`
- Table-driven tests for flag parsing and output
- Test both JSON and table output modes

## Progress Tracking

- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with + prefix
- Document issues/blockers with ! prefix

## Implementation Steps

### Task 1: rum command group and search subcommand

- [x] write tests for `rum search`: parses --query, --from, --to, --limit, --sort flags
- [x] write tests for `rum search`: formats RUM events as table (timestamp, type, application, view)
- [x] write tests for `rum search`: outputs JSON with --json flag
- [x] write tests for `rum search`: defaults --from to "now-15m" when omitted
- [x] implement `internal/cmd/rum.go` with `NewRUMCommand()`
- [x] implement `rum search` using `datadogV2.RUMApi.ListRUMEvents()`
- [x] register `rum` command in root command
- [x] run tests - must pass before next task

### Task 2: rum aggregate subcommand

- [x] write tests for `rum aggregate`: parses --query, --from, --to, --group-by, --compute flags
- [x] write tests for `rum aggregate`: formats aggregation results as table
- [x] implement `rum aggregate` using `datadogV2.RUMApi.AggregateRUMEvents()`
- [x] run tests - must pass before next task

### Task 3: rum app subcommands

- [x] write tests for `rum app list`: outputs table with id, name, type, client token
- [x] write tests for `rum app show <id>`: outputs application detail
- [x] write tests for `rum app create`: parses --name, --type flags
- [x] write tests for `rum app update <id>`: parses update flags
- [x] write tests for `rum app delete <id>`: requires --yes flag
- [x] implement subcommands using `datadogV2.RUMApi` application methods
- [x] run tests - must pass before next task

### Task 4: rum metric subcommands

- [x] write tests for `rum metric list`: outputs table with id, compute, filter, group-by
- [x] write tests for `rum metric show <id>`: outputs metric detail
- [x] write tests for `rum metric create`: parses --id, --compute, --filter, --group-by flags
- [x] write tests for `rum metric update <id>`: parses update flags
- [x] write tests for `rum metric delete <id>`: requires --yes flag
- [x] implement subcommands using `datadogV2.RumMetricsApi`
- [x] run tests - must pass before next task

### Task 5: rum retention-filter subcommands

- [x] write tests for `rum retention-filter list --app <id>`: outputs table
- [x] write tests for `rum retention-filter show --app <id> <filter-id>`
- [x] write tests for `rum retention-filter create --app <id>`: parses flags
- [x] write tests for `rum retention-filter update --app <id> <filter-id>`
- [x] write tests for `rum retention-filter delete --app <id> <filter-id>`
- [x] implement subcommands using `datadogV2.RumRetentionFiltersApi`
- [x] run tests - must pass before next task

### Task 6: rum playlist subcommands

- [x] write tests for `rum playlist list`: outputs table with id, name, created
- [x] write tests for `rum playlist show <id>`: outputs playlist detail
- [x] write tests for `rum playlist create`: parses --name flag
- [x] write tests for `rum playlist update <id>`: parses update flags
- [x] write tests for `rum playlist delete <id>`: requires --yes flag
- [x] write tests for `rum playlist sessions <id>`: lists sessions in playlist
- [x] write tests for `rum playlist add-session <playlist-id> <session-id>`
- [x] write tests for `rum playlist remove-session <playlist-id> <session-id>`
- [x] implement subcommands using `datadogV2.RumReplayPlaylistsApi`
- [x] run tests - must pass before next task

### Task 7: rum heatmap subcommands

- [x] write tests for `rum heatmap list --view <name>`: outputs table
- [x] write tests for `rum heatmap create`: parses snapshot config flags
- [x] write tests for `rum heatmap update <id>`: parses update flags
- [x] write tests for `rum heatmap delete <id>`: requires --yes flag
- [x] implement subcommands using `datadogV2.RumReplayHeatmapsApi`
- [x] run tests - must pass before next task

### Task 8: rum session subcommands

- [x] write tests for `rum session segments --view <view-id> --session <session-id>`
- [x] write tests for `rum session watchers <session-id>`: lists watchers
- [x] write tests for `rum session watch <session-id>`: records a watch
- [x] write tests for `rum session history`: lists viewed sessions
- [x] implement subcommands using `RumReplaySessionsApi` and `RumReplayViewershipApi`
- [x] run tests - must pass before next task

### Task 9: rum audience subcommands

- [ ] write tests for `rum audience connections list --entity <entity>`
- [ ] write tests for `rum audience connections create --entity <entity>`
- [ ] write tests for `rum audience connections update --entity <entity>`
- [ ] write tests for `rum audience connections delete <id> --entity <entity>`
- [ ] write tests for `rum audience mapping --entity <entity>`
- [ ] write tests for `rum audience query-users`: parses filter flags
- [ ] write tests for `rum audience query-accounts`: parses filter flags
- [ ] implement subcommands using `datadogV2.RumAudienceManagementApi`
- [ ] run tests - must pass before next task

### Task 10: Verify acceptance criteria

- [ ] verify all rum subcommands are registered and show in help
- [ ] verify search, aggregate, app CRUD work with mock data
- [ ] verify replay and audience commands handle errors correctly
- [ ] run full test suite (`go test -race ./...`)
- [ ] run linter (`golangci-lint run`)

### Task 11: Update documentation

- [ ] update README.md with rum command reference

## Technical Details

### CLI structure

```
dd rum search --query "type:view" --from "now-1h" --limit 50
dd rum aggregate --query "type:error" --compute "count" --group-by "@view.url"
dd rum app list|show|create|update|delete
dd rum metric list|show|create|update|delete
dd rum retention-filter list|show|create|update|delete --app <id>
dd rum playlist list|show|create|update|delete
dd rum playlist sessions|add-session|remove-session
dd rum heatmap list|create|update|delete
dd rum session segments|watchers|watch|history
dd rum audience connections list|create|update|delete
dd rum audience mapping|query-users|query-accounts
```

### Search output columns

Table mode: `TIMESTAMP | TYPE | APPLICATION | VIEW | DURATION`
