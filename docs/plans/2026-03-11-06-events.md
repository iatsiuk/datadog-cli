# Events: search, list, get, create

## Overview

Implement the `events` command group for datadog-cli, providing CLI access to
Datadog Events APIs. Covers listing, searching, creating, and viewing events.

Uses `github.com/DataDog/datadog-api-client-go/v2` (datadogV2.EventsApi).

Depends on: foundation plan.

## Context

- SDK APIs used:
  - `datadogV2.EventsApi`: ListEvents, SearchEvents, GetEvent, CreateEvent (+ pagination variants)

## Development Approach

- **Testing approach**: TDD (tests first)
- Complete each task fully before moving to the next
- **CRITICAL: every task MUST include new/updated tests**
- **CRITICAL: all tests must pass before starting next task**

## Testing Strategy

- Mock HTTP responses using `httptest.Server`
- Table-driven tests for flag parsing and output
- Test both JSON and table output modes

## Progress Tracking

- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with + prefix
- Document issues/blockers with ! prefix

## Implementation Steps

### Task 1: events command group and list subcommand

- [x] write tests for `events list`: parses --query, --from, --to, --limit, --sort flags
- [x] write tests for `events list`: formats events as table (timestamp, title, source, tags)
- [x] write tests for `events list`: outputs JSON with --json flag
- [x] write tests for `events list`: defaults --from to "now-24h" when omitted
- [x] implement `internal/cmd/events.go` with `NewEventsCommand()`
- [x] implement `events list` using `datadogV2.EventsApi.ListEvents()`
- [x] register `events` command in root command
- [x] run tests - must pass before next task

### Task 2: events search subcommand

- [x] write tests for `events search`: parses --query (required), --from, --to, --limit flags
- [x] write tests for `events search`: formats results same as list
- [x] implement `events search` using `datadogV2.EventsApi.SearchEvents()`
- [x] run tests - must pass before next task

### Task 3: events show subcommand

- [x] write tests for `events show <id>`: outputs full event detail
- [x] write tests for `events show`: outputs JSON with --json flag
- [x] write tests for `events show`: returns error for non-existent event
- [x] implement `events show` using `datadogV2.EventsApi.GetEvent()`
- [x] run tests - must pass before next task

### Task 4: events create subcommand

- [x] write tests for `events create`: parses --title (required), --text, --tags, --alert-type, --source flags
- [x] write tests for `events create`: outputs created event ID
- [x] write tests for `events create`: validates --alert-type values (info, warning, error, success)
- [x] implement `events create` using `datadogV2.EventsApi.CreateEvent()`
- [x] run tests - must pass before next task

### Task 5: events tail subcommand

- [x] write tests for `events tail`: parses --query flag
- [x] write tests for `events tail`: polls API at interval and prints new events
- [x] implement `events tail` using `datadogV2.EventsApi.ListEvents()` in a poll loop
- [x] run tests - must pass before next task

### Task 6: Verify acceptance criteria

- [x] verify all events subcommands are registered and show in help
- [x] verify list, search, show, create, tail work with mock data
- [x] verify error handling for missing required flags
- [x] run full test suite (`go test -race ./...`)
- [x] run linter (`golangci-lint run`)

### Task 7: Update documentation

- [x] update README.md with events command reference

## Technical Details

### CLI structure

```
dd events list --query "source:github" --from "now-24h" --to "now" --limit 50
dd events search --query "deployment" --from "now-7d"
dd events show <event-id>
dd events create --title "Deploy v2.1" --text "Released to prod" --tags "env:prod,service:web" --alert-type info
dd events tail --query "source:pagerduty"
```

### List output columns

Table mode: `TIMESTAMP | TITLE | SOURCE | TAGS`

### Show output fields

Detail mode: ID, Title, Text, Date, Source, Tags, Alert Type, URL
